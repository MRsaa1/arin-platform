# 🔑 Как добавить API ключи

## Где вносить ключи

Отредактируйте файл `.env` в корне проекта:

```bash
cd ~/arin-platform
nano .env
# или
code .env
# или любой другой редактор
```

## Какие ключи нужно добавить

### 1. NVIDIA API Key (обязательно для DeepSeek R1)
```bash
NVIDIA_API_KEY=nvapi-ваш-ключ-здесь
```

**Где получить:**
- Зарегистрируйтесь на https://build.nvidia.com/
- Получите бесплатный API ключ
- Вставьте в `.env` файл

### 2. OpenAI API Key (опционально, для GPT-4 fallback)
```bash
OPENAI_API_KEY=sk-ваш-ключ-здесь
```

**Где получить:**
- https://platform.openai.com/api-keys
- Создайте новый ключ
- Вставьте в `.env` файл

## Пример .env файла

```bash
# API Keys
NVIDIA_API_KEY=nvapi-1234567890abcdef...
OPENAI_API_KEY=sk-1234567890abcdef...

# Остальные настройки уже есть в файле
```

## После добавления ключей

Перезапустите backend:
```bash
docker-compose restart backend
```

Или пересоздайте контейнер:
```bash
docker-compose down
docker-compose up -d
```

## Проверка

Проверьте что ключи загружены:
```bash
docker-compose logs backend | grep -i "api key"
```

