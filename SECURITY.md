# Security Policy

## 🔒 Безопасность данных

Этот репозиторий **НЕ содержит** чувствительных данных:
- ✅ API ключи хранятся только в переменных окружения (`.env` файл, который НЕ включен в репозиторий)
- ✅ Пароли БД задаются через переменные окружения
- ✅ Все секреты исключены через `.gitignore`

## ⚠️ Важно перед использованием

1. **Создайте `.env` файл** на основе `.env.example`:
   ```bash
   cp .env.example .env
   ```

2. **Замените все placeholder значения** на реальные:
   - `NVIDIA_API_KEY` - ваш NVIDIA API ключ
   - `OPENAI_API_KEY` - ваш OpenAI API ключ
   - `POSTGRES_PASSWORD` - надежный пароль для БД
   - `NEO4J_PASSWORD` - надежный пароль для Neo4j
   - `SECRET_KEY` - случайная строка для JWT

3. **Никогда не коммитьте `.env` файл** в Git!

## 🛡️ Best Practices

- Используйте разные пароли для development и production
- Регулярно ротируйте API ключи
- Используйте secrets management (HashiCorp Vault, AWS Secrets Manager) в production
- Включите `.env` в `.gitignore` (уже включен)

## 📝 Reporting Security Issues

Если вы обнаружили уязвимость безопасности, пожалуйста:
1. НЕ создавайте публичный issue
2. Свяжитесь с maintainer через GitHub или email
3. Опишите проблему детально

## ✅ Проверка перед коммитом

Перед каждым коммитом убедитесь:
- [ ] Нет реальных API ключей в коде
- [ ] Нет паролей в конфигурационных файлах
- [ ] `.env` файл не включен
- [ ] Все секреты используют переменные окружения

