# 🔒 Security Fix - Password Removal

## Проблема
GitGuardian обнаружил общие пароли в репозитории.

## Исправления

### 1. Удалены дефолтные пароли из docker-compose.yml
- ❌ Было: `POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-arin}`
- ✅ Стало: `POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}` (обязательная переменная)

### 2. Удалены дефолтные пароли из docker-compose.prod.yml
- ❌ Было: `NEO4J_PASSWORD:-password`
- ✅ Стало: `NEO4J_PASSWORD` (обязательная переменная)

### 3. Обновлен .env.example
- ❌ Было: реальные пароли `arin`
- ✅ Стало: `CHANGE_THIS_STRONG_PASSWORD` с предупреждениями

## Требования

**ВСЕ пароли теперь обязательны через переменные окружения!**

Перед запуском установите:
```bash
export POSTGRES_PASSWORD="your-strong-password"
export TIMESCALEDB_PASSWORD="your-strong-password"
export NEO4J_PASSWORD="your-strong-password"
export SECRET_KEY="your-generated-secret-key"
```

Или создайте `.env` файл (НЕ коммитьте его в Git!):
```bash
cp .env.example .env
# Отредактируйте .env и замените все CHANGE_THIS_* на реальные значения
```

## Проверка

Запустите проверку безопасности:
```bash
python scripts/verify_production_readiness.py
```

## Важно

- ❌ НИКОГДА не коммитьте `.env` файлы
- ❌ НИКОГДА не используйте дефолтные пароли в production
- ✅ Используйте сильные уникальные пароли
- ✅ Используйте secrets manager в production

