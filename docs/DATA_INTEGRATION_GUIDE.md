# 📊 Руководство по подключению данных к ARIN Platform

## 🎯 Текущий статус

**ARIN Platform работает**, но для полноценного анализа рисков нужны **реальные данные**:
- ✅ Агенты запущены и работают
- ✅ API доступен
- ✅ Frontend подключен
- ⚠️ **Нужны данные**: портфель, баланс компании, рыночные данные

## 🔌 Варианты подключения данных

### 1. **Подключение портфеля (Market Risk)**

Для анализа рыночного риска нужны данные о портфеле:

```bash
# Пример запроса на анализ портфеля
POST https://arin.saa-alliance.com/api/v1/risks/analyze
{
  "type": "market_risk",
  "entity_id": "portfolio_001",
  "entity_type": "portfolio",
  "parameters": {
    "positions": [
      {"symbol": "AAPL", "quantity": 100, "price": 150.0},
      {"symbol": "GOOGL", "quantity": 50, "price": 2800.0}
    ],
    "portfolio_value": 200000
  }
}
```

**Что нужно:**
- Список позиций (активы, количество, цена)
- Общая стоимость портфеля
- Исторические данные для расчета VaR

### 2. **Подключение баланса компании (Credit Risk)**

Для анализа кредитного риска нужны финансовые данные компании:

```bash
# Пример запроса на анализ компании
POST https://arin.saa-alliance.com/api/v1/risks/analyze
{
  "type": "credit_risk",
  "entity_id": "company_001",
  "entity_type": "company",
  "parameters": {
    "balance_sheet": {
      "assets": 1000000,
      "liabilities": 600000,
      "equity": 400000
    },
    "income_statement": {
      "revenue": 500000,
      "expenses": 400000,
      "net_income": 100000
    },
    "cash_flow": {
      "operating": 120000,
      "investing": -50000,
      "financing": -20000
    }
  }
}
```

**Что нужно:**
- Балансовый отчет (Balance Sheet)
- Отчет о прибылях и убытках (Income Statement)
- Отчет о движении денежных средств (Cash Flow)

### 3. **Интеграция с внешними системами**

ARIN Platform поддерживает интеграцию с:

#### a) Investment Dashboard
```env
INVESTMENT_DASHBOARD_URL=http://your-investment-dashboard.com
```

#### b) Risk Analyzer
```env
RISK_ANALYZER_URL=http://your-risk-analyzer.com
```

#### c) Прямое подключение к БД

Настройте подключение к вашей базе данных в `.env`:
```env
DATABASE_URL=postgresql://user:password@host:port/database
```

## 📝 Как добавить данные

### Вариант 1: Через API (рекомендуется)

Используйте API endpoints для загрузки данных:

```bash
# Загрузка портфеля
POST /api/v1/graph/nodes
{
  "node_id": "portfolio_001",
  "node_type": "portfolio",
  "properties": {
    "value": 200000,
    "positions": [...]
  }
}

# Загрузка компании
POST /api/v1/graph/nodes
{
  "node_id": "company_001",
  "node_type": "company",
  "properties": {
    "balance_sheet": {...},
    "income_statement": {...}
  }
}
```

### Вариант 2: Прямое подключение к БД

1. Настройте подключение к вашей БД
2. Агенты автоматически будут читать данные из таблиц:
   - `portfolios` - портфели
   - `companies` - компании
   - `financial_statements` - финансовые отчеты
   - `market_data` - рыночные данные

### Вариант 3: Интеграция через файлы

Загрузите данные через CSV/JSON файлы:

```bash
POST /api/v1/data/upload
Content-Type: multipart/form-data
file: portfolio_data.csv
```

## 🚀 Быстрый старт

### Шаг 1: Подготовьте данные

Соберите данные о:
- Портфеле (позиции, стоимость)
- Компаниях (баланс, отчеты)
- Рыночных данных (цены, объемы)

### Шаг 2: Загрузите через API

```bash
curl -X POST https://arin.saa-alliance.com/api/v1/risks/analyze \
  -H "Content-Type: application/json" \
  -d '{
    "type": "market_risk",
    "entity_id": "my_portfolio",
    "entity_type": "portfolio",
    "parameters": {
      "positions": [...],
      "portfolio_value": 1000000
    }
  }'
```

### Шаг 3: Просмотрите результаты

Откройте https://arin.saa-alliance.com/dashboard и перейдите на вкладку "Risks" или "Graph"

## 📊 Примеры данных

### Портфель
```json
{
  "portfolio_id": "portfolio_001",
  "value": 1000000,
  "positions": [
    {
      "symbol": "AAPL",
      "quantity": 100,
      "price": 150.0,
      "weight": 0.15
    },
    {
      "symbol": "GOOGL",
      "quantity": 50,
      "price": 2800.0,
      "weight": 0.14
    }
  ],
  "currency": "USD"
}
```

### Компания
```json
{
  "company_id": "company_001",
  "name": "Example Corp",
  "balance_sheet": {
    "total_assets": 1000000,
    "total_liabilities": 600000,
    "total_equity": 400000
  },
  "income_statement": {
    "revenue": 500000,
    "expenses": 400000,
    "net_income": 100000
  }
}
```

## ⚙️ Настройка интеграций

### Investment Dashboard

Если у вас есть Investment Dashboard, настройте интеграцию:

```env
INVESTMENT_DASHBOARD_URL=https://your-dashboard.com
```

ARIN автоматически будет получать данные о портфелях.

### Risk Analyzer

Если у вас есть Risk Analyzer, настройте:

```env
RISK_ANALYZER_URL=https://your-analyzer.com
```

ARIN будет использовать результаты анализа из Risk Analyzer.

## 🔍 Проверка подключения

```bash
# Проверка агентов
curl https://arin.saa-alliance.com/api/v1/agents

# Проверка графа
curl https://arin.saa-alliance.com/api/v1/graph

# Тестовый анализ
curl -X POST https://arin.saa-alliance.com/api/v1/risks/analyze \
  -H "Content-Type: application/json" \
  -d '{"type": "market_risk", "entity_id": "test", "entity_type": "portfolio"}'
```

## 📌 Важно

1. **Без данных** система работает, но показывает mock/тестовые результаты
2. **С данными** система выполняет реальный анализ рисков
3. **Интеграция** с внешними системами ускоряет получение данных
4. **API ключи** (NVIDIA, OpenAI) нужны для LLM анализа

## 🎯 Следующие шаги

1. Подготовьте данные о портфеле/компании
2. Загрузите через API или подключите БД
3. Запустите анализ через Dashboard или API
4. Просмотрите результаты в Dashboard

---

**Система готова к работе!** Просто добавьте данные. 🚀

